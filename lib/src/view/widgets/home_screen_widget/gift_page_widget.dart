import 'package:flutter/material.dart';

class GiftPageWidget extends StatelessWidget {
  const GiftPageWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Gift page'),
    );
  }
}